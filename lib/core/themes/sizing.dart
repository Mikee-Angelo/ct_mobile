import 'package:flutter/material.dart';

class Sizing {
  const Sizing._();

  static const basePadding = EdgeInsets.symmetric(horizontal: 16);
  static const inputBorder = BorderRadius.all(Radius.circular(7.0));
}
